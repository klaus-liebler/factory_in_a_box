# tools/test_opcua.py -- SecurityPolicy#Basic256Sha256 needs real RSA sign/verify/OAEP, which the
# stdlib doesn't provide. pip install -r tools/requirements.txt
cryptography
# tools/test-https-and-websocket.py -- RFC6455 WebSocket client (HTTPS itself uses stdlib ssl/
# http.client).
websockets
