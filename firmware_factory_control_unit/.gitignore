build
mx.scratch
!.settings

# Generierter Code (s. docs/build-process.md) -- wird von den builder/-Phasen erzeugt, nicht
# eingecheckt. Quelle der Wahrheit ist jeweils das Board-Archiv unter stm32_boards/<id>/generated/.
# build/assets/ (Geraetezertifikat/-schluessel, index.html.br) ist bereits durch die "build"-Regel
# oben abgedeckt, extra Eintrag nicht noetig.
/Core/generated/
/web/generated/
/web/dist/
node_modules/

# builder/ -- appsettings.json enthaelt persoenliche Maschinenpfade und ist gitignored, getrackt
# ist nur appsettings.json.template; builder.csproj legt die reale Datei bei Bedarf automatisch
# daraus an (s. docs/build-process.md).
/builder/appsettings.json
/builder/bin/
/builder/obj/
__pycache__/
/tools/.opcua-test-client/